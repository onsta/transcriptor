export enum font {
    remSize = "5px",
    size = "3rem",
    height = "4rem",
}

export enum padding {
    small = "3rem",
    big = "6rem",
}

export enum container {
    maxWidth = "240rem",
    minWidth = "50rem",
}
